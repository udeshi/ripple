# Ripple Web

Next.js web client for Ripple.

## Development

```bash
npm install
npm run dev
```

Open [http://localhost:3000](http://localhost:3000).

The app expects the API (see `../backend`) running at `http://localhost:3001/api`.
